export const customPropNames = {
    action: 'action',
    route: 'route',
    response: 'response'
}

export const networkParams = {
    port: 'port',
    root: 'root'
}

export const defaultPort = 3000

export const defaultSseRoot = '/sse'

export const eventTypes = {
    shutdownApp: 'shutdown-app',
    pushSse: 'push-sse'
}